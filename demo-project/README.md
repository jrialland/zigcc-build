# Demo Package

A demonstration package built with zigcc-build backend.

This package includes both C extensions and pure Python code, showcasing the capabilities of the zigcc-build backend.

## Features

- C extension compiled with Zig CC
- Pure Python package support
- Dynamic configuration via configure.py
